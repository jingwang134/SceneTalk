# 🎬 ScenePick · 原生观点范例「AI 生成规则」（RULES-v2）

> 本文件是「原生观点范例」页内容的唯一生成规范。所有 AI 生成的维度与语料必须遵守。
> 违反规则的内容会被 `check_persp.js` 校验脚本拦截。

---

## 一、核心理念

**思维导图式，不是列表式。**

```
话题
├── 维度① 家人各有闪光点（思维角度）
│   ├── phrase  短语语料（紧贴维度）
│   ├── sentence 台本真实单句（紧贴维度）
│   └── clip    完整观点片段（紧贴维度）
│   └── use     雅思支架句（怎么把这个维度用进回答）
├── 维度② ……
└── 维度③ ……
```

❌ 错误示范（v1 的病）：角度列表和片段各玩各的，角度说"能力匹配vs热爱"，配的片段却是"ABC销售"。
✅ 正确示范（v2）：语料必须**服务于维度观点**。"ABC销售"片段只能挂在"谈判说服"维度下。

---

## 二、结构规范（每条必填）

### 1. 话题层（8 个话题：family/work/communication/values/education/skills/friendship/media）
| 字段 | 说明 |
|------|------|
| `map` | 一句话标题："聊「X」，外国人通常从这 3 个维度展开" |

### 2. 维度层（每话题固定 3 个维度）
| 字段 | 说明 |
|------|------|
| `tag` | "维度①" 等序号 |
| `name` | 维度名（8-12字，点明该维度核心对立/关键词，如"能力匹配 vs 内心热爱"） |
| `angle` | 该维度母语者思维，大白话 1-2 句，面向雅思考生 |
| `use` | **雅思支架句**：把该维度组织进 Part 2/3 回答的模板句，必须内嵌该维度 1 个核心表达 |

### 3. 语料层（`items`，每维度 2-4 条，三级递进）
| 类型 | 定义 | 要求 |
|------|------|------|
| `phrase` | 短语/习语（来自表达库） | 标注来源 |
| `sentence` | 台本真实单句 | 必须是台本原句，标注集数，配 `tip` 讲解 |
| `clip` | 完整观点片段（2-5句连续台词） | 必须是台本原句，标注集数，配 `tip` 讲结构 |

**硬性要求：**
- 每维度**至少 1 条 `sentence` 或 `clip`**（禁止只有短语）
- `tip` 必须讲"母语者怎么组织语言/结构"（如：先观点后例子、对比结构、先破后立），**不许只翻译**

---

## 三、语义规则（校验脚本强制）

1. **紧贴维度**：items 的语义必须与 `angle`/`name` 观点一致。
   - 校验方法：脚本提取维度关键词（vs 前后词、核心表达），与每条语料的 en/zh 做关键词重叠检测；重叠为 0 判定"脱节"，自动拦截。
2. **来源真实**：sentence/clip 必须逐字来自台本（scripts/s01eXX.txt），脚本会做原文比对。
3. **来源标注**：`source` 格式统一为「剧名 S01EXX」或「多部美剧通用」（表达库通用条）。
4. **不重复**：同一话题 3 个维度之间，核心短语不重复；同一短语在不同话题可复用，但每条必须重新服务于该维度观点。

---

## 四、AI 生成流程（后续自动化的标准动作）

```
输入：台本全文（scripts/s01eXX.txt）+ 本规则
1. 通读全集 → 找出 2-4 个"母语者观点完整表达"的段落
2. 归纳每个段落的思维角度 → 映射到 8 话题之一，归入对应维度（缺维度可新增，但每话题上限 3）
3. 从段落中切分语料：短语 → 句子 → 完整片段
4. 写 tip（语言结构讲解）+ use（雅思支架）
5. 跑 check_persp.js → 全绿才可合入
```

**禁止事项：**
- ❌ 虚构台词（sentence/clip 必须能在台本中检索到原文）
- ❌ 角度与语料脱节（校验拦截）
- ❌ 每话题超过 3 个维度（防止膨胀）
- ❌ 把"思维""框架"等抽象词直接甩给用户（页面话术必须具象）
