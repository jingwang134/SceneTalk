# 🎬 ScenePick · 原生观点范例「AI 生成规则」（RULES-v2）

> 本文件是「原生观点范例」页内容的唯一生成规范。所有 AI 生成的维度与语料必须遵守。
> 违反规则的内容会被 `check_persp_v2.js` 校验脚本拦截。

---

## 〇、产品定位（先想清楚再生成）

**这个页面不是"母语者思维研究"，就是一本"从剧里提炼的同场景地道表达库"。**

- 素材来源 = 美剧台本，没有任何"母语思维"玄学，**不宣称某种说法是唯一的**
- 每个维度给用户**一批同场景的说法**，挑顺手的用，多多益善
- 页面话术一律用"谈 XX 时，可以这么说/可以聊 XX"——**禁用词见下文，出现即报错**

---

## 一、核心理念

**思维导图式，不是列表式。**

```
话题
├── 维度① 家人各有闪光点（场景分组）
│   ├── phrase  短语语料（紧贴维度）
│   ├── sentence 台本真实单句（紧贴维度）
│   └── clip    完整观点片段（紧贴维度）
│   └── use     支架句（怎么把这个维度用进回答）
├── 维度② ……
└── 维度③ ……
```

❌ 错误示范（v1 的病）：角度列表和片段各玩各的，角度说"能力匹配vs热爱"，配的片段却是"ABC销售"。
✅ 正确示范（v2）：语料必须**服务于维度观点**。"ABC销售"片段只能挂在"谈判说服"维度下。

---

## 二、结构规范（每条必填）

### 1. 话题层（8 个话题：family/work/communication/values/education/skills/friendship/media）
| 字段 | 说明 |
|------|------|
| `map` | 一句话标题："聊「X」，可以从这 3 个维度展开" |

### 2. 维度层（每话题固定 3 个维度）
| 字段 | 说明 |
|------|------|
| `tag` | "维度①" 等序号 |
| `name` | 维度名（8-12字，点明该维度核心对立/关键词，如"能力匹配 vs 内心热爱"） |
| `angle` | **切入引导话术**：用"谈 XX 时，可以这么说"句式，告诉用户这个场景有哪些说法可用（1-2 句大白话，面向雅思考生） |
| `use` | **支架句**：把该维度组织进 Part 2/3 回答的模板句，必须内嵌该维度 1 个核心表达 |

`angle` 文案示例（✅ 正确 / ❌ 错误）：
- ✅ "谈工作选择时，可以聊'能不能胜任'和'喜不喜欢'这两方面，下面给你几种说法。"
- ❌ "聊工作选择，母语者先谈'能力匹配'，再谈'内心热爱'。"（不要假装在讲母语者思维）

### 3. 语料层（`items`，每维度 2-4 条，三级递进）
| 类型 | 定义 | 要求 |
|------|------|------|
| `phrase` | 短语/习语（来自表达库） | 标注来源 |
| `sentence` | 台本真实单句 | 必须是台本原句，标注集数，配 `tip` 讲解 |
| `clip` | 完整观点片段（2-5句连续台词） | 必须是台本原句，标注集数，配 `tip` 讲结构 |

**硬性要求：**
- 每维度**至少 1 条 `sentence` 或 `clip`**（禁止只有短语）
- `tip` 必须讲"这句话/这段怎么组织的"（如：先观点后例子、对比结构、先破后立），**不许只翻译**
- 每个维度只是"一批同场景的说法"，**不要暗示只有这一种说法**（避免"就只能这么说"的绝对化措辞）

---

## 三、语义规则（校验脚本强制）

1. **紧贴维度**：items 的语义必须与 `angle`/`name` 观点一致。
   - 校验方法：脚本提取维度关键词，与每条语料的 en/zh 做重叠检测；提示人工复核语义贴合度。
2. **来源真实**：sentence/clip 必须能在台本（scripts/s01eXX.txt）中检索到——**AI 不得改写、补全、润色台词**（实词顺序比对，容忍台本双语混排/换行/字母间隔）。
3. **来源标注**：`source` 格式统一为「剧名 S01EXX」或「多部美剧通用」（表达库通用条）。
4. **不重复**：同一话题 3 个维度之间，核心短语不重复；同一短语在不同话题可复用，但每条必须重新服务于该维度观点。
5. **禁用措辞（出现即硬错误）**：
   - ❌ "母语者"（如"母语者常…""母语思维"）
   - ❌ "思维框架""观察思维"等抽象概念词
   - ❌ 绝对化表达（"唯一的说法""只能这么说"）

---

## 四、AI 生成流程（后续自动化的标准动作）

```
输入：台本全文（scripts/s01eXX.txt）+ 本规则
1. 通读全集 → 找出 2-4 个"观点表达完整"的段落
2. 归纳每个段落的场景/话题 → 映射到 8 话题之一，归入对应维度（缺维度可新增，但每话题上限 3）
3. 从段落中切分语料：短语 → 句子 → 完整片段（逐字，不得改写）
4. 写 angle（"谈XX时，可以这么说"引导话术）+ tip（结构讲解）+ use（支架句）
5. 跑 check_persp_v2.js → 全绿才可合入
```

**禁止事项：**
- ❌ 虚构/改写台词（sentence/clip 必须在台本中检索到原文）
- ❌ 角度与语料脱节（校验拦截）
- ❌ 每话题超过 3 个维度（防止膨胀）
- ❌ 出现"母语者/思维框架/唯一的说法"等措辞（校验拦截）

---

## 附录 C：场景数据规范（剧集式学习 · RULES-v3）

**目标**：让表达"回到剧情场景"——用户学表达时先看到它在剧里怎么被说出来的。

**数据结构**（SCENES_DEAL，挂到表达 id 上）：
```js
{ id: 33, context: ["台词行1 中文翻译", "...", "命中句 中文翻译", "..."], hit: 2 }
// context = 命中句前后共 5-7 行双语台词；hit = 命中句在 context 中的索引
```

**生成方式**（`python mine_scenes.py`，半自动）：
1. 每条表达**人工指定搜索词**（2-3 个，含变体，如 shake → shook/shaken）
2. 按 source 剧名**绑定目录**（摩登家庭=scripts/，查理=scripts/glc/，卡戴珊=scripts/kardashian/）——⚠️ 必须绑定，否则会跨剧误匹配同名 sXXeYY.txt
3. 归一化匹配（去标点/大小写），整句精确优先、拆词兜底
4. 人工审一遍输出，确认命中句确实是该表达

**铁律**：
- 上下文必须是**台本逐字原句**（含中文翻译），禁止改写、禁止 AI 补写台词
- 无台本的来源（如 Suits）不生成场景，页面自动降级为普通卡片（sceneOf 返回 null）
- 前端：sceneOf(id) + sceneBlockHtml(e) 渐进增强，闪卡正面显示「🎬 这句出自剧中场景」徽章，背面显示场景区块，命中句金色高亮
- 落地脚本：land_scene.py（幂等，可重复跑）
