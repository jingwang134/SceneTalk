# 🎬 ScenePick · 其他电脑继续迭代指南

> 在新电脑上继续开发 / 更新 SceneTalk 仓库的完整流程。
> 全程只要会复制粘贴命令，不需要很深的技术背景。

---

## 第一步 · 新电脑准备（一次性，约10分钟）

### 1. 安装 Git
- 下载：https://git-scm.com/download/win（一路 Next 装完）
- 装完打开 **Git Bash**（开始菜单搜索）

### 2. 生成 SSH 密钥
```bash
ssh-keygen -t ed25519 -C "jingwang134@users.noreply.github.com"
# 一路回车即可（不用设密码）
```

### 3. 把公钥添加到 GitHub
```bash
cat ~/.ssh/id_ed25519.pub
```
复制输出的整行内容 → 打开 GitHub → **Settings → SSH and GPG keys → New SSH key** → 粘贴 → Add

### 4. 配置 git 身份（以后提交署名用）
```bash
git config --global user.name "jingwang134"
git config --global user.email "jingwang134@users.noreply.github.com"
```

---

## 第二步 · 解决国内网络连不上 GitHub（关键！）

国内网络直连 GitHub 的 22 端口经常超时，需要走 **443 端口**。在 Git Bash 执行：

```bash
echo '
Host github443
  HostName ssh.github.com
  Port 443
  User git
  IdentityFile ~/.ssh/id_ed25519' >> ~/.ssh/config
```

测试是否成功（看到 `Hi jingwang134!` 就通了）：
```bash
ssh -T github443
```

> ⚠️ 后面所有命令里凡是 `github.com` 的地方，都用 `github443` 代替。

---

## 第三步 · 克隆仓库（新电脑只需做一次）

```bash
git clone git@github443:jingwang134/SceneTalk.git
cd SceneTalk
```

---

## 第四步 · 日常迭代（三种模式，按需选）

### 🥇 模式A：只传台本（最简单，不需要代码能力）

这是"在其他电脑加学习资料"的推荐方式，**全程网页操作**：

1. 浏览器打开 https://github.com/jingwang134/SceneTalk
2. 点 **Add file → Upload files**
3. 把台本文件（txt/pdf）拖进页面，**路径填 `uploads/`**（会自动建文件夹）
4. 点 **Commit changes** 提交
5. 在 WorkBuddy 里说：**"处理 uploads 里的台本"** → AI 提取雅思表达、更新 `data.js` 并推送
6. 刷新在线页面即可看到新表达

### 🥈 模式B：直接改表达数据（data.js）

所有表达数据都在 **`data.js`** 里。两种改法：
- **网页改**（少量）：GitHub 网页打开 `data.js` → ✏️ 编辑 → 在数组末尾按格式加对象 → Commit
- **本地改**（批量）：克隆后编辑 `data.js`，格式见文件顶部注释

### 🥉 模式C：改代码（完整开发）

编辑 `index.html` / `data.js` 后：
```bash
git add -A
git commit -m "本次改动说明"
git push origin main
```

本地预览效果：
```bash
# 方式1：直接双击 index.html
# 方式2：起本地服务器（推荐，与线上一致）
python -m http.server 8000
# 浏览器打开 http://localhost:8000
```

---

## 第五步 · 常用命令速查

| 想做什么 | 命令 |
|---------|------|
| 看看改了什么 | `git status` |
| 拉取线上最新（别人/AI改的） | `git pull origin main` |
| 提交所有改动 | `git add -A && git commit -m "说明"` |
| 推送到线上 | `git push origin main` |
| 查看提交历史 | `git log --oneline` |

---

## ❓ 常见问题

**Q: push 报错 "rejected" / "non-fast-forward"？**
A: 线上有别人改过 → 先 `git pull origin main` 合并，再 push。

**Q: 改了 data.js 但页面没变？**
A: 强制刷新浏览器：`Ctrl + F5`（清缓存）。

**Q: 忘了 git 身份配置？**
A: 执行第一步第4条的两条命令即可。

**Q: 想从线上恢复某文件？**
A: `git checkout origin/main -- 文件名`

**Q: 预览时打卡/XP 数据丢了？**
A: 打卡数据存在浏览器 localStorage（本机），换电脑/浏览器会重新开始，属正常现象。

---

## 目录结构速览

```
SceneTalk/
├── index.html   # 界面 + 全部逻辑（表达库/训练场/词库/打卡）
├── data.js      # 表达数据（78条+，可独立编辑）
├── uploads/     # （自己建）放待处理的台本
├── parts/       # 工程片段（供脚本重建用）
├── scripts/     # 已提取的台本文本
└── *.py *.js    # 工程脚本（校验/插入/重建）
```
